set_cam_zoom.hx________PACKSEP________function onEvent(_) {
	if (_.event.name == 'set_cam_zoom') {
		defaultCamZoom = Std.parseFloat(_.event.params[0]);
	}
}________PACKSEP________{
	"params": [
		{
			"name": "zoom",
			"type": "String",
			"defaultValue": 1
		}
	]
}